name: Sync LiteLLM callback docs

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/sync-litellm-callback-docs.yml"

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Synchronize README and inference-gateway docs
        run: python scripts/sync_litellm_callback_docs.py
      - name: Documentation parity
        run: |
          python scripts/sync_i18n_nav.py --check
          python scripts/audit_i18n_parity.py --languages fr de ru
      - name: Remove one-time synchronizer and commit
        run: |
          rm scripts/sync_litellm_callback_docs.py
          rm .github/workflows/sync-litellm-callback-docs.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "docs: mark LiteLLM callback automatic in live"
          git push origin HEAD:main
